import {motion} from "framer-motion";
import React, {ReactNode} from "react";

const MagicalMover = ({children}: {children: ReactNode}) => {
  return <motion.div>MagicalMover</motion.div>;
};

export default MagicalMover;
